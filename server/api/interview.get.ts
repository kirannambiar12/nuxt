export default defineEventHandler(() => {
  return {
    description: "This payload is returned from a Nitro server endpoint.",
    highlights: [
      "File-based routing in pages/",
      "Server API handlers in server/api/",
      "SSR + CSR + static rendering in one app",
      "Dynamic routes with params"
    ]
  };
});
